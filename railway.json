{
    "build": {
        "builder": "nixpacks"
    },
    "deploy": {
        "startCommand": "php artisan storage:link && php artisan migrate --force && php artisan optimize && php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckPath": "/",
        "restartPolicyType": "always"
    }
}